[tool.poetry]
name = "twiliosmshack"
version = "0.1.0"
description = ""
authors = ["SUKHJITSINGH <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
twilio = "^7.2.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
